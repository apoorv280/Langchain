# python -m venv venv
# venv\Scripts\activate
# pip install -r requirements.txt
# to update pip 
# python.exe -m pip install --upgrade pip
# python -m specifies which interpreter to use
# py -3.11 -m venv my_env
